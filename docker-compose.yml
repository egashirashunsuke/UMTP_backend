services:
  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: server
    environment:
      TZ: Asia/Tokyo
      PORT: 8000 
    tty: true
    volumes:
      - ./:/app
    ports:
      - "8000:8000"

